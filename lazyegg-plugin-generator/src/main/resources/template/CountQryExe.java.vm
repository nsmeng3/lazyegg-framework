package ${package}.${moduleName}.${entityNameLowercase}.executor.query;

import com.alibaba.cola.dto.SingleResponse;
import ${package}.${moduleName}.domain.${entityNameLowercase}.gateway.${entityClassName}Gateway;
import ${package}.${moduleName}.dto.${entityClassName}CountQry;
import ${package}.${moduleName}.dto.data.${entityClassName}DTO;
import lombok.Data;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;


@Component
public class ${entityClassName}CountQryExe {

    @Autowired
    private ${entityClassName}Gateway ${entityName}Gateway;

    public ${entityClassName}DTO execute(${entityClassName}CountQry qry) {
        long count = ${entityName}Gateway.getCount();
        ${entityClassName}DTO ${entityName}DTO = new ${entityClassName}DTO();
        return ${entityName}DTO;
    }

    @Data
    public static class Context {

    }
}
