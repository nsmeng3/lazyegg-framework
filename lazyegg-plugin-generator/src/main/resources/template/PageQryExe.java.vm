package ${package}.${moduleName}.${entityName}.executor.query;

import com.alibaba.cola.dto.MultiResponse;
import ${package}.${moduleName}.domain.${entityName}.${entityClassName};
import ${package}.${moduleName}.domain.${entityName}.gateway.${entityClassName}Gateway;
import ${package}.${moduleName}.dto.${entityClassName}PageQry;
import ${package}.${moduleName}.dto.data.${entityClassName}DTO;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;


@Component
public class ${entityClassName}PageQryExe {

    @Autowired
    private ${entityClassName}Gateway ${entityName}Gateway;

    public MultiResponse<${entityClassName}DTO> execute(${entityClassName}PageQry qry) {
        List<${entityClassName}> ${entityName}List = ${entityName}Gateway.page${entityClassName}(qry.getPageIndex(), qry.getPageSize());

        return MultiResponse.of(new ArrayList<>());
    }
}
