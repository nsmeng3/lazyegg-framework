package ${package}.${moduleName}.${entityName}.executor;

import com.alibaba.cola.dto.Response;
import ${package}.${moduleName}.domain.${entityName}.gateway.${entityClassName}Gateway;
import ${package}.${moduleName}.dto.${entityClassName}UpdateCmd;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

import javax.annotation.Resource;

/**
 * ${entityClassName}UpdateCmdExe
 *
 * @author DifferentW  nsmeng3@163.com
 */

@Component
public class ${entityClassName}UpdateCmdExe {
    private static final Logger log = LoggerFactory.getLogger(${entityClassName}UpdateCmdExe.class);

    @Resource
    private ${entityClassName}Gateway ${entityName}Gateway;

    public Response execute(${entityClassName}UpdateCmd cmd) {
        ${entityName}Gateway.update${entityClassName}(cmd);
        return Response.buildSuccess();
    }
}

