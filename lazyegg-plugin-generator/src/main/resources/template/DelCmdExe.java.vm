package ${package}.${moduleName}.${entityName}.executor;

import com.alibaba.cola.dto.Response;
import ${package}.${moduleName}.domain.${entityName}.gateway.${entityClassName}Gateway;
import ${package}.${moduleName}.dto.${entityClassName}DelCmd;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

import javax.annotation.Resource;

/**
 * ${entityClassName}DelCmdExe
 *
 * @author DifferentW  nsmeng3@163.com
 */

@Component
public class ${entityClassName}DelCmdExe {
    private static final Logger log = LoggerFactory.getLogger(${entityClassName}DelCmdExe.class);

    @Resource
    private ${entityClassName}Gateway ${entityName}Gateway;

    public Response execute(${entityClassName}DelCmd ${entityName}DelCmd) {
        ${entityName}Gateway.remove${entityClassName}(${entityName}DelCmd);
        return Response.buildSuccess();
    }
}

